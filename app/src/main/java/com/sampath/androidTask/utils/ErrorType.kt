package com.sampath.androidTask.utils

import com.sampath.androidTask.R

enum class ErrorType(val title: Int, val message: Int) {
    UNKNOWN(R.string.unknown_error_title, R.string.unknown_error_message)
}
